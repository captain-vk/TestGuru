<div class="row justify-content-center mt-3">
  <div class="col col-md-4">
    <h2 class="text-info"><%= t('.header') %></h2>
    <table>
    <tr>
      <th><%= t('.title') %></th>
      <th><%= t('.category') %></th>
      <th><%= t('.level') %></th>
      <th><%= t('.questions') %></th>      
      <th><%= t('.actions') %></th>
    </tr>
      <%= render @tests %>
    </table>
  </div>
</div> 