<div class="row justify-content-center mt-3">
  <div class="col col-md-4">
    <% flash.each do |key, message| %>
      <%= content_tag :p, message, class: "flash #{bootstrap_flash(key)}"  %>
    <% end %>
  </div>
</div>